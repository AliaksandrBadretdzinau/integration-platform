# My ExpressJS project with Postgres wrapped in Docker

Steps to run this project:

1. Run `docker build -t integration-platform .` command (ensure you have docker installed)
2. Run `docker-compose up` command
